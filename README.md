# 🔊 Voice Room

Голосовые комнаты для общения в реальном времени - веб и Android приложение на базе WebRTC и Socket.IO.

## Возможности

- 🎤 **Голосовое общение** - общение в реальном времени через WebRTC
- 👥 **Многопользовательские комнаты** - до 10 участников в комнате
- 📱 **Кроссплатформенность** - веб-версия и Android приложение
- 🔄 **Автоматическое переподключение** - надежное соединение
- 🎨 **Современный UI** - адаптивный дизайн для всех устройств
- 🔒 **Безопасность** - валидация данных, защита от XSS

## Быстрый старт

### Локальная разработка

1. **Клонируйте репозиторий:**
```bash
git clone <repository-url>
cd voice-room-app
```

2. **Установите зависимости:**
```bash
npm install
```

3. **Запустите сервер:**
```bash
npm run dev
```

Сервер будет доступен на `http://localhost:3000`

### Использование Docker

1. **Соберите образ:**
```bash
docker build -t voice-room .
```

2. **Запустите контейнер:**
```bash
docker-compose up -d
```

Или с переменными окружения:
```bash
PORT=3000 docker-compose up -d
```

### Настройка переменных окружения

Скопируйте `.env.example` в `.env` и настройте:

```bash
cp .env.example .env
```

Редактируйте `.env`:
```env
PORT=3000
HOST=0.0.0.0
CORS_ORIGIN=*
MAX_USERS_PER_ROOM=10
ROOM_TIMEOUT_MINUTES=30
```

## Сборка Android приложения

### Требования

- Node.js (v18+)
- Java JDK (v11+)
- Android SDK и Gradle
- Cordova CLI (устанавливается автоматически)

**Важно:** Для сборки APK требуется установка Gradle. Рекомендуется установить Android Studio, который включает все необходимые инструменты.

### Установка Gradle

**Вариант 1: Android Studio (рекомендуется)**
1. Скачайте и установите [Android Studio](https://developer.android.com/studio)
2. После установки Gradle будет автоматически доступен в PATH
3. Android Studio также включает Android SDK и эмулятор

**Вариант 2: Gradle отдельно**
1. Скачайте Gradle с [официального сайта](https://gradle.org/releases/)
2. Распакуйте архив
3. Добавьте `bin` папку в PATH системных переменных
   - Пример: `C:\gradle\bin`

**Вариант 3: Через пакетные менеджеры Windows**
```powershell
# Chocolatey
choco install gradle

# Scoop
scoop install gradle
```

### Сборка

1. **Добавьте Android платформу (если еще не добавлена):**
```bash
npm run init
```

2. **Соберите APK:**
```bash
npm run build
```

APK файл будет находиться в:
```
platforms/android/app/build/outputs/apk/debug/app-debug.apk
```

После сборки APK также копируется в корень проекта:
```
app-debug.apk    (для debug сборки - подписан автоматически, готов к установке)
app-release.apk  (для release сборки - может быть неподписанным)
```

**Важно:** Для установки на устройства рекомендуется использовать debug APK, так как он автоматически подписывается debug keystore. Release APK требует дополнительной настройки подписи.

3. **Релизная сборка:**
```bash
npm run build:release
```

### Загрузка APK на сервер

После сборки APK локально, его нужно загрузить на сервер для скачивания через сайт:

**Вариант 1: Использовать скрипт upload-apk.sh**
```bash
# Установите sshpass для автоматической загрузки (опционально)
# Linux: sudo apt-get install sshpass
# macOS: brew install hudochenkov/sshpass/sshpass

# Загрузите APK на сервер (автоматически найдет debug APK если доступен)
bash upload-apk.sh
```

**Вариант 2: Использовать PowerShell скрипт (Windows)**
```powershell
npm run upload:apk
# Или напрямую:
.\upload-apk.ps1
```

**Вариант 3: Загрузка вручную через scp**
```bash
# Debug APK (рекомендуется для тестирования)
scp app-debug.apk root@82.146.44.126:/opt/voice-room/app-debug.apk

# Release APK
scp app-release.apk root@82.146.44.126:/opt/voice-room/app-release.apk
# Пароль: carFds43
```

**Вариант 4: Загрузка через GitHub Actions**
Если APK файл есть в репозитории, он автоматически будет скопирован при деплое.

После загрузки APK будет доступен по адресу: `https://aiternitas.ru/download/apk`

**Примечание:** Сборка APK на сервере не выполняется автоматически, так как требует установки Android SDK и Gradle. Рекомендуется собирать APK локально и загружать на сервер. Используйте debug APK для тестирования - он автоматически подписывается и готов к установке.

### Установка на устройство

1. **Подключите устройство по USB:**
   - Включите "Отладку по USB" в настройках разработчика
   - Разрешите доступ к устройству

2. **Установите и запустите:**
```bash
npm run install:device
```

Или полный цикл (сборка + установка + запуск):
```bash
npm run run
```

3. **Проверка устройства:**
```bash
npm run check-device
```

### Запуск в эмуляторе

```bash
npm run run:emulator
```

## Автоматический деплой

При каждом push в ветку `master` автоматически запускается деплой на сервер через GitHub Actions.

**Настройка GitHub Secrets:**
1. Перейдите в Settings → Secrets → Actions
2. Добавьте секреты:
   - `SSH_PASSWORD` = пароль для SSH (carFds43)
   - `GIT_REPO_URL` = https://github.com/ebusorgin/timer_apk.git (опционально)

**Важно:** APK файл не собирается автоматически на сервере. Если нужен APK для скачивания:
1. Соберите APK локально: `npm run build`
2. Загрузите на сервер: `bash upload-apk.sh`
3. Или добавьте APK файл в репозиторий для автоматического деплоя

## Деплой на production сервер

Подробные инструкции по деплою на сервер aiternitas.ru смотрите в [DEPLOY.md](DEPLOY.md)

**Быстрый деплой:**
```bash
# На сервере
ssh root@82.146.44.126
cd /opt/voice-room
bash deploy.sh main
```

## Структура проекта

```
voice-room-app/
├── www/                    # Веб-файлы (используются веб и Cordova)
│   ├── css/
│   ├── js/
│   └── index.html
├── server/
│   └── server.mjs         # Node.js сервер (Socket.IO)
├── platforms/             # Cordova платформы (генерируется)
├── package.json           # Зависимости проекта
├── Dockerfile             # Для деплоя
├── docker-compose.yml     # Для локальной разработки
└── README.md
```

## API и использование

### Создание комнаты

1. Откройте веб-интерфейс или запустите Android приложение
2. Введите ваше имя
3. Нажмите "Создать комнату"
4. Скопируйте ссылку или код комнаты
5. Поделитесь с другими участниками

### Присоединение к комнате

1. Введите код комнаты (6 символов)
2. Введите ваше имя
3. Нажмите "Присоединиться"

### Настройка сервера для мобильных

В Android приложении можно указать IP адрес сервера для подключения с телефона:

1. На экране входа введите IP адрес сервера (например: `192.168.1.100:3000`)
2. Нажмите кнопку сохранения
3. Приложение переподключится к указанному серверу

## Деплой на сервер

### Docker

```bash
# Соберите образ
docker build -t voice-room .

# Запустите контейнер
docker run -d \
  -p 3000:3000 \
  -e PORT=3000 \
  -e CORS_ORIGIN=https://yourdomain.com \
  --name voice-room \
  voice-room
```

### Docker Compose

```bash
# С переменными окружения из .env
docker-compose up -d
```

### PM2 (альтернатива)

```bash
# Установите PM2
npm install -g pm2

# Запустите сервер
pm2 start server/server.mjs --name voice-room

# Сохраните конфигурацию
pm2 save
pm2 startup
```

## Переменные окружения

| Переменная | Описание | По умолчанию |
|-----------|----------|--------------|
| `PORT` | Порт сервера | `3000` |
| `HOST` | Хост сервера | `0.0.0.0` |
| `CORS_ORIGIN` | Разрешенные источники CORS | `*` |
| `MAX_USERS_PER_ROOM` | Максимум пользователей в комнате | `10` |
| `ROOM_TIMEOUT_MINUTES` | Таймаут пустой комнаты (минуты) | `30` |

## Разработка

### Структура кода

- `www/js/voice-room.js` - основной модуль Voice Room
- `www/js/app.js` - конфигурация приложения
- `server/server.mjs` - Socket.IO сервер

### Добавление функций

1. Внесите изменения в код
2. Для веб-версии: сервер автоматически обновит файлы
3. Для Android: выполните `npm run build` для пересборки

### Тестирование

```bash
# Линтинг
npm run lint

# Проверка устройства
npm run check-device
```

## Технические детали

- **WebRTC** - для P2P голосового соединения
- **Socket.IO** - для сигнализации и координации
- **Express** - веб-сервер
- **Cordova** - для Android приложения

### WebRTC серверы

Приложение использует:
- STUN серверы Google для обнаружения NAT
- TURN серверы OpenRelay для обхода сложных NAT
- Автоматическое переподключение при потере соединения

## Безопасность

- Валидация всех входных данных
- Санитизация строк для защиты от XSS
- Ограничение количества пользователей в комнате
- Автоматическая очистка пустых комнат
- Ограничение CORS по доменам в production

## Устранение неполадок

### Сервер не запускается

1. Проверьте, что порт свободен: `lsof -i :3000` (Linux/Mac) или `netstat -ano | findstr :3000` (Windows)
2. Измените порт через переменную окружения: `PORT=3001 npm run dev`

### Устройство не подключается

1. Проверьте, что сервер доступен по сети: `npm run check-device`
2. Убедитесь, что телефон и компьютер в одной сети Wi-Fi
3. Проверьте firewall настройки

### Проблемы со сборкой Android

1. Убедитесь, что установлен Android SDK
2. Проверьте переменные окружения: `ANDROID_HOME` или `ANDROID_SDK_ROOT`
3. Выполните: `npm run check-device` для диагностики

## Лицензия

ISC

## Контрибьюция

Приветствуются pull requests и issues!
